<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoshu.dao.StudentMapper">
  <resultMap id="BaseResultMap" type="com.xiaoshu.entity.Student">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="sid" jdbcType="INTEGER" property="sid" />
    <result column="sname" jdbcType="VARCHAR" property="sname" />
    <result column="sex" jdbcType="VARCHAR" property="sex" />
    <result column="pic" jdbcType="VARCHAR" property="pic" />
    <result column="time" jdbcType="DATE" property="time" />
    <result column="bid" jdbcType="INTEGER" property="bid" />
  </resultMap>
  <resultMap id="sab" type="com.xiaoshu.entity.Student">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="sid" jdbcType="INTEGER" property="sid" />
    <result column="sname" jdbcType="VARCHAR" property="sname" />
    <result column="sex" jdbcType="VARCHAR" property="sex" />
    <result column="pic" jdbcType="VARCHAR" property="pic" />
    <result column="time" jdbcType="DATE" property="time" />
    <result column="bid" jdbcType="INTEGER" property="bid" />
    <association javaType="com.xiaoshu.entity.Bzr" property="bzr">
		<id column="bid" property="bid" />
		<result column="bname" property="bname" />
	</association>
  </resultMap>
  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <!--
      WARNING - @mbg.generated
    -->
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
    -->
    sid, sname, sex, pic, time, bid
  </sql>
  <select id="findStudent" resultMap="sab" parameterType="Student">
  select*from student s,bzr b 
  <where>
  s.bid=b.bid
  <if test="sname!=null and sname!=''">
   and	s.sname like '%${sname}%'
  </if>
  <if test="bid!=null and bid!=0">
  	and s.bid=#{bid}
  </if>
  </where>
  </select>
  <select id="findTj" resultType="Tj">
  SELECT COUNT(*) AS num,b.bname FROM student s,bzr b WHERE s.bid=b.bid GROUP BY b.bname
  </select>
</mapper>